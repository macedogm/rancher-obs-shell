workflow:
  steps:
    - branch_package:
        source_project: home:gmacedo:rancher
        source_package: shell
        target_project: home:gmacedo:rancher:images-test
  filters:
    event: pull_request
